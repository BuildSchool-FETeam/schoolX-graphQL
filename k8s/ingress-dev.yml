apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: prisdom-ingress
  namespace: dev
spec:
  ingressClassName: nginx
  rules:
  - host: gql.prisdom-dev.online
    http:
      paths:
      - pathType: Prefix
        path: /
        backend:
          service:
            name: graphql-svc
            port:
              number: 3001