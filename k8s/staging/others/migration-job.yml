apiVersion: batch/v1
kind: Job
metadata:
  name: migration
  namespace: staging
spec:
  completions: 1
  ttlSecondsAfterFinished: 30
  template:
    spec:
      volumes:
      - name: migration-storage
        persistentVolumeClaim:
          claimName: migrate-claim-staging
      containers:
      - name: staging-mirgate-job
        image: prisdom/graphql:latest-staging
        # The volume only using for migrations-revert
        volumeMounts:
        - mountPath: /tmp
          name: migration-storage
        command: 
          - bash
          - k8s/scripts/run-migration.sh
        envFrom:  
        - configMapRef:  
            name: app-config
        - secretRef:
            name: be-credentials

      restartPolicy: Never
  backoffLimit: 2