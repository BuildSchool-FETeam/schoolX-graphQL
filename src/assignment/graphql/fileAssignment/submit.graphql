type SubmitType implements BaseGraphQL {
    id: ID!
    title: String!
    createdAt: ScalarDate!
    updatedAt: ScalarDate!
    description: String
    order: Int!
    url: String!
    student: StudentType!
}