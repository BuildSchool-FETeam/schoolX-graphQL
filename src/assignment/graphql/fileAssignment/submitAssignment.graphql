type SubmitAssignmentType implements BaseGraphQL{
    id: ID!
    title: String!
    createdAt: ScalarDate!
    updatedAt: ScalarDate!
    description: String
    order: Int!
    fileUrl: String!
    student: StudentType!
    comments: [UserCommentType]
}

input SubmitAssignmentSetInput {
    title: String!
    description: String
    files: [Upload]
}