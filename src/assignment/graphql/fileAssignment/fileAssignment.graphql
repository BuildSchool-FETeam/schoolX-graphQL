type FileAssignmentType implements BaseGraphQL {
    id: ID!
    title: String!
    createdAt: ScalarDate!
    updatedAt: ScalarDate!
    description: String!
    maxScore: Int!
    estimateTime: Int!
    instructions: String
    explain: String
    studentSubmitted: [StudentType]
}